# API Документация

## Базовый URL

Все API эндпоинты находятся по пути `/api/...`

Локально: `http://127.0.0.1:8000/api/...`

## Формат ответов

### Успешный ответ

```json
{
  "ok": true,
  "id": 1
}
```

### Ошибка

```json
{
  "error": "Описание ошибки"
}
```

---

## Аутентификация

### POST /api/auth/request-code

Запрос кода для входа на email.

**Тело запроса:**
```json
{
  "email": "user@example.com",
  "name": "Имя пользователя" // опционально
}
```

**Ответ:**
```json
{
  "ok": true,
  "demo_code": "123456" // только если DEV_DELIVERY=true
}
```

**Ошибки:**
- `400` — Неверный email
- `500` — Ошибка БД или отправки email

**Особенности:**
- Если пользователь не существует и указано имя, создается новый пользователь
- Код действителен 10 минут
- В демо-режиме код возвращается в ответе

---

### POST /api/auth/verify-code

Проверка кода и создание сессии.

**Тело запроса:**
```json
{
  "email": "user@example.com",
  "code": "123456"
}
```

**Ответ:**
```json
{
  "ok": true
}
```

**Cookies (устанавливаются автоматически):**
- `sid` — ID сессии (HttpOnly, SameSite=Lax)
- `u` — JSON с email и name (SameSite=Lax)

**Ошибки:**
- `400` — Неверный или истекший код

**Особенности:**
- Сессия действует 7 дней
- Cookie `u` доступен из JavaScript для отображения имени пользователя

---

### POST /api/auth/logout

Выход из системы.

**Ответ:**
```json
{
  "ok": true
}
```

**Особенности:**
- Удаляет сессию из БД
- Очищает cookies `sid` и `u`

---

## Пользователи (Users)

### GET /api/users

Получить список всех пользователей.

**Ответ:**
```json
{
  "users": [
    {
      "id": 1,
      "name": "Иван Иванов",
      "email": "ivan@example.com",
      "created_at": "2024-01-15T10:30:00+00:00"
    }
  ]
}
```

**Особенности:**
- Сортировка по ID (DESC)
- Защищено middleware (требует авторизации)

---

### GET /api/users?id=123

Получить пользователя по ID.

**Параметры:**
- `id` (query) — ID пользователя (целое число)

**Ответ:**
```json
{
  "user": {
    "id": 1,
    "name": "Иван Иванов",
    "email": "ivan@example.com",
    "created_at": "2024-01-15T10:30:00+00:00"
  }
}
```

**Ошибки:**
- `400` — ID не является целым числом
- `null` в поле `user` — пользователь не найден

---

### POST /api/users

Создать нового пользователя.

**Тело запроса:**
```json
{
  "name": "Иван Иванов",
  "email": "ivan@example.com"
}
```

**Ответ:**
```json
{
  "ok": true,
  "id": 1
}
```

**Ошибки:**
- `400` — name и email обязательны
- `409` — Email уже существует

**Особенности:**
- ID генерируется автоматически (BIGSERIAL)
- Email должен быть уникальным

---

### PUT /api/users

Обновить данные пользователя.

**Тело запроса:**
```json
{
  "id": 1,
  "name": "Новое имя",
  "email": "new@example.com"
}
```

**Ответ:**
```json
{
  "ok": true
}
```

**Ошибки:**
- `400` — ID не является целым числом или нечего обновлять
- `404` — Пользователь не найден
- `409` — Email уже существует

**Особенности:**
- Можно обновить только name, только email, или оба поля
- Пустые строки игнорируются

---

### DELETE /api/users?id=123

Удалить пользователя.

**Параметры:**
- `id` (query) — ID пользователя

**Альтернативно (тело запроса):**
```json
{
  "id": 1
}
```

**Ответ:**
```json
{
  "ok": true
}
```

**Ошибки:**
- `400` — ID не является целым числом
- `404` — Пользователь не найден

---

## Товары (Products)

### GET /api/products

Получить список всех товаров.

**Ответ:**
```json
{
  "products": [
    {
      "id": 1,
      "name": "Товар",
      "description": "Описание товара",
      "price_cents": 9999,
      "image_url": "https://example.com/image.jpg"
    }
  ]
}
```

**Особенности:**
- Сортировка по ID (DESC)
- `image_url` может быть `null`

---

### GET /api/products?id=123

Получить товар по ID.

**Параметры:**
- `id` (query) — ID товара (целое число)

**Ответ:**
```json
{
  "product": {
    "id": 1,
    "name": "Товар",
    "description": "Описание",
    "price_cents": 9999,
    "image_url": "https://example.com/image.jpg"
  }
}
```

**Ошибки:**
- `400` — ID не является целым числом
- `null` в поле `product` — товар не найден

---

### POST /api/products

Создать новый товар.

**Тело запроса:**
```json
{
  "name": "Название товара",
  "description": "Подробное описание",
  "price_cents": 9999,
  "image_url": "https://example.com/image.jpg"
}
```

**Ответ:**
```json
{
  "ok": true,
  "id": 1
}
```

**Ошибки:**
- `400` — name, description и price_cents обязательны
- `400` — price_cents должен быть неотрицательным целым числом

**Особенности:**
- ID генерируется автоматически (BIGSERIAL)
- Цена хранится в центах (целое число)
- `image_url` может быть `null`

---

### PUT /api/products

Обновить данные товара.

**Тело запроса:**
```json
{
  "id": 1,
  "name": "Новое название",
  "description": "Новое описание",
  "price_cents": 19999,
  "image_url": "https://example.com/new.jpg"
}
```

**Ответ:**
```json
{
  "ok": true
}
```

**Ошибки:**
- `400` — ID не является целым числом или нечего обновлять
- `400` — price_cents должен быть неотрицательным целым
- `404` — Товар не найден

**Особенности:**
- Можно обновить любое поле или несколько полей
- Пустые строки для `image_url` устанавливают `null`

---

### DELETE /api/products?id=123

Удалить товар.

**Параметры:**
- `id` (query) — ID товара

**Альтернативно (тело запроса):**
```json
{
  "id": 1
}
```

**Ответ:**
```json
{
  "ok": true
}
```

**Ошибки:**
- `400` — ID не является целым числом
- `404` — Товар не найден

---

## Заказы (Orders)

### POST /api/orders

Создать новый заказ.

**Требует аутентификации:** Да (cookie `sid`)

**Тело запроса:**
```json
{
  "items": [
    { "product_id": 1, "qty": 2 },
    { "product_id": 2, "qty": 1 }
  ]
}
```

**Ответ:**
```json
{
  "ok": true,
  "order": {
    "id": 1,
    "user_id": 1,
    "total_cents": 29997,
    "created_at": "2024-01-15T10:30:00+00:00",
    "items_map": {
      "1": {
        "qty": 2,
        "price_cents": 9999,
        "line_total_cents": 19998,
        "name": "Товар 1"
      },
      "2": {
        "qty": 1,
        "price_cents": 9999,
        "line_total_cents": 9999,
        "name": "Товар 2"
      }
    }
  }
}
```

**Ошибки:**
- `401` — Не авторизован
- `400` — items обязательны и не пусты
- `400` — Неверный формат items (product_id и qty должны быть целыми числами)
- `400` — Товары не найдены в БД

**Особенности:**
- Пользователь определяется из сессии (cookie `sid`), поле `user` в теле запроса игнорируется
- Дубликаты `product_id` в `items` суммируются
- Товары проверяются на существование в БД
- `total_cents` пересчитывается на сервере

---

## Коды состояния HTTP

- `200` — Успешный запрос
- `201` — Ресурс создан
- `302` — Редирект на страницу логина (для неавторизованных HTML-запросов)
- `400` — Ошибка валидации
- `401` — Не авторизован (для API-запросов)
- `404` — Ресурс не найден
- `409` — Конфликт (например, дубликат email)
- `500` — Внутренняя ошибка сервера
