meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  persistenceMount: /data
  scriptName: backend/app.py
  containerPort: 80
  servicePort: 80
