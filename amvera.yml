meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  persistenceMount: /data
  command: uvicorn backend.app:app --host 0.0.0.0 --port 80
  containerPort: 80
  servicePort: 80
